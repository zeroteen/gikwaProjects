@model IEnumerable<Grikwa.Models.Institution_Resident>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Institution.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Resident.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Institution.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Resident.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IRID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IRID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IRID })
        </td>
    </tr>
}

</table>
