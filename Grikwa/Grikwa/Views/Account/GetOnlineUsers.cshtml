@model IEnumerable<Grikwa.Models.PendingUser>

@{
    ViewBag.Title = "Online Users";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var count = 0;
}

<h2>@ViewBag.Title</h2>

@foreach (var user in Model)
{
    @(count = count + 1)
    <h5>[@user.UserID] - [@user.FullName]</h5>
}

