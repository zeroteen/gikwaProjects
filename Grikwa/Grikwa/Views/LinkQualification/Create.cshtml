@model Grikwa.Models.Institution_Faculty_Qualification

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Institution_Faculty_Qualification</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.IFID, "IFID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("IFID", String.Empty)
                @Html.ValidationMessageFor(model => model.IFID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.QualificationID, "QualificationID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("QualificationID", String.Empty)
                @Html.ValidationMessageFor(model => model.QualificationID)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
