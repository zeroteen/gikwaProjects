@model IEnumerable<Grikwa.Models.Institution_Faculty>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Faculty.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Institution.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Faculty.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Institution.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IFID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IFID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IFID })
        </td>
    </tr>
}

</table>
